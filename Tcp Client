#TCP Client.py

import socket 

clientsocket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

host = '10.0.0.86'
host = socket.gethostname()

port = 444

clientsocket.connect(('10.0.0.86, port'))

message = clientsocket.recv(1024)

clientsocket.close()

print(message.decode('ascii'))
